// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1

package invoices

import (
	"github.com/google/uuid"
	"github.com/jackc/pgx/v5/pgtype"
)

// Sales invoices an organisation issues to its contacts. Org-scoped, soft-deleted. Header for invoice_items. Models the FreeAgent Invoice resource (minimal first cut).
type Invoice struct {
	ID              uuid.UUID   `json:"id"`
	OrganisationID  uuid.UUID   `json:"organisation_id"`
	CreatedByUserID uuid.UUID   `json:"created_by_user_id"`
	ContactID       uuid.UUID   `json:"contact_id"`
	DatedOn         pgtype.Date `json:"dated_on"`
	DueOn           pgtype.Date `json:"due_on"`
	// Customer-facing invoice number. Nullable until issued (auto-numbering deferred). Unique per org once set (uq_invoices_reference).
	Reference pgtype.Text `json:"reference"`
	Currency  string      `json:"currency"`
	// STORED lifecycle only: DRAFT|SCHEDULED|SENT|WRITTEN_OFF|REFUNDED. The display status (Open/Overdue/Paid/Overpaid/Zero Value) is DERIVED in the service from due_on + total_value_minor + paid_value_minor.
	Status             string `json:"status"`
	NetValueMinor      int64  `json:"net_value_minor"`
	SalesTaxValueMinor int64  `json:"sales_tax_value_minor"`
	// Gross total (net + VAT) in minor units (pence). BIGINT because billing totals can exceed the int32 ceiling. Recomputed from invoice_items on save.
	TotalValueMinor int64 `json:"total_value_minor"`
	PaidValueMinor  int64 `json:"paid_value_minor"`
	// Outstanding amount = total_value_minor - paid_value_minor. GENERATED/STORED so the DB keeps it correct; read-only.
	DueValueMinor pgtype.Int8        `json:"due_value_minor"`
	DeletedAt     pgtype.Timestamptz `json:"deleted_at"`
	CreatedAt     pgtype.Timestamptz `json:"created_at"`
	UpdatedAt     pgtype.Timestamptz `json:"updated_at"`
}

// Line items of an invoice. Child of invoices (ON DELETE CASCADE). Stores raw inputs (quantity, unit price, VAT rate); the service computes line/header totals.
type InvoiceItem struct {
	ID             uuid.UUID `json:"id"`
	InvoiceID      uuid.UUID `json:"invoice_id"`
	OrganisationID uuid.UUID `json:"organisation_id"`
	Position       int32     `json:"position"`
	Description    string    `json:"description"`
	// Line quantity, NUMERIC so it can be fractional (e.g. 2.5). Surfaced as a Go string (numeric override); parse with shopspring/decimal.
	Quantity string `json:"quantity"`
	// Per-unit price in minor units (pence), VAT-EXCLUSIVE. VAT is added on top at sales_tax_rate_bps (NOT extracted — unlike the expenses path).
	PriceMinor      int64              `json:"price_minor"`
	SalesTaxRateBps int32              `json:"sales_tax_rate_bps"`
	CreatedAt       pgtype.Timestamptz `json:"created_at"`
	UpdatedAt       pgtype.Timestamptz `json:"updated_at"`
}
